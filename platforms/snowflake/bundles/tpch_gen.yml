bundle:
  type: spark
  compute_type: warehouse
  language: python
  compute_options: {}
  properties:
    python_dependencies:
      packages:
        - tpchgen-cli          # Rust TPC-H generator, installed into the job's sandbox
